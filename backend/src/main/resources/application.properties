spring.application.name=booking-system

# Server
server.port=${PORT:8080}

# MySQL
spring.datasource.url=mysql://root:TrOZAyqIYlKifkVzhYVYogDgrulDDqzR@hopper.proxy.rlwy.net:55979/railway
spring.datasource.username=root
spring.datasource.password=TrOZAyqIYlKifkVzhYVYogDgrulDDqzR
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${APP_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}

# JWT
app.jwt.secret=${APP_JWT_SECRET:beautybook-dev-secret-key-change-me-before-production}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}

# Email (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=${MAIL_USERNAME:your-email@gmail.com}
